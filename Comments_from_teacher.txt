
После выхода из with соединение с клиентом будет разорвано. Чтобы клиент мог отправлять 
внутри одного сокета несколько клиентов, надо чтобы сервер умел их считывать. 
Для текущего задания это не нужно. Если хотите оставить 3 сообщения, то надо будет на 
каждый открывать новый сокет со стороны клиента.

Не забывайте либо s.close() после завершения работы, либо 
with socket(AF_INET, SOCK_STREAM) as s:

